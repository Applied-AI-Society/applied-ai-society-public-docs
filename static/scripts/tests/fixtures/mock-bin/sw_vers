#!/usr/bin/env bash
echo "sw_vers $*" >> "${MOCK_CALL_LOG:-/dev/null}"
case "${1:-}" in
  -productVersion)
    echo "${MOCK_MACOS_VERSION:-14.0}"
    ;;
  *)
    echo "ProductName: macOS"
    ;;
esac
